import styled from 'styled-components';

const StyledWrapper = styled.div`
  .summary {
    margin-top: 50px;
    h1 {
      text-align: center;
      margin: 0px;
      padding-inline: 10px;
    }
  }
`;

export default StyledWrapper;
